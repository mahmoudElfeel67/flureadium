name: flutter_readium_platform_interface
description: A common platform interface for the flutter_readium plugin.
version: 0.0.1
homepage: https://github.com/notalib/flutter_readium
repository: https://github.com/notalib/flutter_readium
publish_to: none

environment:
  sdk: ">=3.8.0 <4.0.0"
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.17.0
  json_diff:
  plugin_platform_interface: ^2.1.4
  dartx: ^1.2.0
  dfunc: ^0.10.0
  fimber: ^0.7.0
  equatable: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  analyzer: ^10.0.1
  build_runner:

export type BasicTextSelection = {
  text: string;
  x: number;
  y: number;
};

export enum Width {
  Wrap = 'Wrap',
}

export enum Layout {
  Bounds = 'Bounds',
}

export type FrameClickEvent = any;
